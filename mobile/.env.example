# Expo inlines any EXPO_PUBLIC_ var at build time — copy to .env and point
# this at your backend (e.g. your laptop's LAN IP so a phone on the same
# wifi can reach it: http://192.168.1.23:4000, not localhost).
EXPO_PUBLIC_API_URL="http://localhost:3000"
